if (true) console.log("hello")
else console,log("goodbye")

console.log(true? "hello": "goodbye")  // can be used without {} if the statement contains only one condition

const x=1 
const plural = x<1 ? "s" : ""      // ternary ... same as if (?) and else(:)
console.log("the sting has "+ "vowel" +plural)
